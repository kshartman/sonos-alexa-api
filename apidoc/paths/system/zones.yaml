get:
  tags:
    - System
  summary: List all zones
  description: Returns all zone groups with their members and coordinators
  operationId: getZones
  responses:
    '200':
      description: List of zones
      content:
        application/json:
          schema:
            type: array
            items:
              $ref: '../../components/schemas.yaml#/schemas/Zone'