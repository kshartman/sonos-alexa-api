get:
  tags:
    - Room
  summary: Play SiriusXM station
  description: |
    **NOT IMPLEMENTED** - This endpoint exists for API compatibility but always returns 501.
    SiriusXM integration would require channel list and subscription.
  operationId: playSiriusXM
  parameters:
    - $ref: '../../components/parameters.yaml#/parameters/room'
    - $ref: '../../components/parameters.yaml#/parameters/name'
  responses:
    '501':
      description: Not implemented
      content:
        application/json:
          schema:
            type: object
            properties:
              status:
                type: string
                example: error
              error:
                type: string
                example: SiriusXM is not implemented